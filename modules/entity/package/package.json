{
  "name": "@redux/entity",
  "version": "5.1.0",
  "description": "Common utilities for entity reducers",
  "main": "./dist/public_api.js",
  "scripts": {
    "rollup": "rimraf dist && rollup -c rollup.config.js",
    "bundle": "rimraf dist && rollup -c rollup.config.js && tsc",
    "build": "rimraf dist && tsc"
  },
  "typings": "./dist/public_api.d.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/BeaveArony/platform.git"
  },
  "authors": [
    "Mike Ryan",
    "Michael Gustmann"
  ],
  "license": "MIT",
  "peerDependencies": {
    "reselect": "^3.0.1",
    "rxjs": "^5.5.2"
  },
  "devDependencies": {
    "rollup": "^0.56.3",
    "rimraf":"^2.6.2",
    "typescript": "^2.7.2"
  }
}
